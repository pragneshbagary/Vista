# ============================================================================
# VISTA Personal AI RAG System - Frontend Production Environment Configuration
# ============================================================================
# This file serves as a template for frontend production environment variables.
# Copy this file to .env.production.local and fill in all required values.
# NEVER commit actual secrets to version control.
#
# Validation Rules:
# - All NEXT_PUBLIC_* variables are exposed to the browser
# - API URL must be a valid HTTPS URL in production
# - All URLs must be valid and accessible
# - Email must be a valid email address format
# ============================================================================

# ============================================================================
# API CONFIGURATION (REQUIRED)
# ============================================================================
# Backend API URL: URL where the frontend can reach the backend API
# Validation: Must be a valid HTTPS URL in production
# Format: https://api.yourdomain.com or https://yourdomain.com/api
# Note: This is exposed to the browser, so it must be publicly accessible
NEXT_PUBLIC_VISTA_API_URL=https://api.yourdomain.com

# ============================================================================
# SOCIAL LINKS CONFIGURATION (Optional but recommended)
# ============================================================================
# GitHub URL: link to GitHub profile
# Validation: Must be a valid GitHub profile URL
# Format: https://github.com/username
# Note: Leave empty to hide GitHub link from landing page
NEXT_PUBLIC_GITHUB_URL=https://github.com/yourusername

# LinkedIn URL: link to LinkedIn profile
# Validation: Must be a valid LinkedIn profile URL
# Format: https://www.linkedin.com/in/yourprofile/
# Note: Leave empty to hide LinkedIn link from landing page
NEXT_PUBLIC_LINKEDIN_URL=https://www.linkedin.com/in/yourprofile/

# Email: contact email address
# Validation: Must be a valid email address format
# Format: user@example.com
# Note: Leave empty to hide email from landing page
NEXT_PUBLIC_EMAIL=your.email@example.com

# ============================================================================
# DEPLOYMENT NOTES
# ============================================================================
# 1. REQUIRED VARIABLES:
#    - NEXT_PUBLIC_VISTA_API_URL (must be valid HTTPS URL)
#
# 2. OPTIONAL VARIABLES:
#    - NEXT_PUBLIC_GITHUB_URL (can be empty)
#    - NEXT_PUBLIC_LINKEDIN_URL (can be empty)
#    - NEXT_PUBLIC_EMAIL (can be empty)
#
# 3. SECURITY BEST PRACTICES:
#    - All NEXT_PUBLIC_* variables are exposed to the browser
#    - Do NOT put secrets in these variables
#    - Use HTTPS for all URLs in production
#    - Validate all URLs are accessible before deployment
#
# 4. VALIDATION:
#    - API URL must be a valid HTTPS URL
#    - Social links must be valid URLs
#    - Email must be a valid email address
#    - All URLs must be publicly accessible
#
# 5. DEPLOYMENT:
#    - For Vercel: Set these as environment variables in Vercel dashboard
#    - For Docker: Pass as build arguments or environment variables
#    - For static hosting: Build with these values set
#
# 6. TESTING:
#    - Test API connectivity after deployment
#    - Verify social links are correct
#    - Test error handling for unreachable API
# ============================================================================
