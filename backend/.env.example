# LLM Configuration
# Choose your LLM provider: 'openai' or 'gemini'
LLM_PROVIDER=gemini

# API Keys (only the one for your chosen provider is required)
OPENAI_API_KEY=your_openai_api_key_here
GEMINI_API_KEY=your_gemini_api_key_here

# Model Configuration (optional - defaults are set per provider)
# For OpenAI: gpt-4o-mini, gpt-4o, gpt-3.5-turbo, etc.
# For Gemini: gemini-2.5-flash, gemini-1.5-pro, etc.
LLM_MODEL=gemini-2.5-flash

# Embedding Model
EMBEDDING_MODEL=all-MiniLM-L6-v2

# Pinecone Configuration (REQUIRED)
# Pinecone is a cloud-native vector database for production deployments
# Setup: https://www.pinecone.io/
#
# PINECONE_API_KEY: Your Pinecone API key
# Validation: Must be a valid Pinecone API key
# Format: Usually a long alphanumeric string
PINECONE_API_KEY=your_pinecone_api_key_here

# PINECONE_ENVIRONMENT: Your Pinecone environment (region)
# Validation: Must be a valid Pinecone environment
# Examples: us-west-2-aws, us-east-1-aws, eu-west-1-aws, etc.
# Find your environment at: https://app.pinecone.io/
PINECONE_ENVIRONMENT=us-east-1

# PINECONE_INDEX_NAME: Name of the Pinecone index (optional)
# Validation: Must be a valid index name (lowercase, alphanumeric, hyphens)
# Default: vista-vectors (if not specified)
# Note: Index will be created automatically if it doesn't exist
PINECONE_INDEX_NAME=vista-vectors

# PINECONE_NAMESPACE: Namespace for multi-tenant deployments (optional)
# Validation: Must be a valid namespace string
# Default: default (if not specified)
# Note: Useful for isolating data in shared indexes
PINECONE_NAMESPACE=default

# Directory Configuration
DATA_DIRECTORY=./data

# Chunking Configuration
CHUNK_SIZE=500
CHUNK_OVERLAP=50

# Query Configuration
MAX_CONTEXT_TOKENS=3000
TOP_K_RESULTS=5

# Server Configuration
ENVIRONMENT=development
PORT=8000
LOG_LEVEL=info

# CORS Configuration (comma-separated list of allowed origins)
# For development: http://localhost:3000
# For production: https://yourdomain.com
ALLOWED_ORIGINS=http://localhost:3000

# Frontend Configuration
NEXT_PUBLIC_VISTA_API_URL=http://localhost:8000
NEXT_PUBLIC_GITHUB_URL=https://github.com/yourusername
NEXT_PUBLIC_LINKEDIN_URL=https://www.linkedin.com/in/yourprofile/
NEXT_PUBLIC_EMAIL=your.email@example.com